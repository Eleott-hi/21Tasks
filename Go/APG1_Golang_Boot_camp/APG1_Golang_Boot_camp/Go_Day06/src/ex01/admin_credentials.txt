admin_username: admin
admin_password: superhero_password
db_user: postgres
db_password: postgres
db_name: postgres

-- SQL scripts

DROP TABLE IF EXISTS articles;

CREATE TABLE articles (
    id SERIAL PRIMARY KEY,
    title TEXT NOT NULL,
    content TEXT NOT NULL,
    created_at TIMESTAMPTZ DEFAULT NOW()
);

